# visual-rdbms
A super simple visual RDBMS made in Python using Pygame. It was a school project and I wanted to stitch up an end-to-end system in a week, so the code is an absolute mess... The UI is really pretty though.
